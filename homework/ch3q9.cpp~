#include <iostream>
#include <vector>
using namespace std;

template<class obj>
vector<object> & Read()
{

  obj x;
  vector<object> r;
  cout << "Input an arbitrary number of objects\n";
  cin >> x;
  while( cin >> x )
    {
      r.push_back(x);
    }

  return r;

}
